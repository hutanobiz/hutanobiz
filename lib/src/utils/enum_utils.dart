enum ResetType { password, pin }

enum VerificationScreen { resetPassword, resetPin, registration }

enum VerificationType { email, phone }

enum SearchScreenFrom { addMore, inviteFamily }

enum SetupScreenFrom { login }

enum GenderType { transgender, female, male }

enum RegisterError{firstName , lastName ,email ,password ,address, city,zipCode}


